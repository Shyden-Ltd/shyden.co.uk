---
// Route only. The markup lives in ONE component shared with the Indonesian
// route, so a change to the tool cannot land in one language and not the other.
import ClassroomGroupsPage from '../components/pages/ClassroomGroupsPage.astro';
---

<ClassroomGroupsPage lang="en" />
